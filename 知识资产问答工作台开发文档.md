# 知识资产问答工作台开发文档

## 1. 项目背景

本项目来自企业级 AI Agent 与数字资产知识库平台相关笔试题。题目要求在 4 小时内实现一个简单的 Web App：**知识资产问答工作台**。

该项目主要考察候选人的以下能力：

- 全栈开发能力；
- 前端审美与产品完成度；
- AI 产品理解；
- 检索链路意识；
- 工程结构与代码质量；
- 对 Agent / RAG 基本流程的理解；
- 对真实 ToB 场景中权限、安全、可靠性、可观测性的判断。

本项目不追求复杂功能堆砌，而是强调：**可运行、结构清晰、逻辑可解释、UI 有产品感、README 说明完整**。

------

## 2. 项目目标

实现一个轻量级的企业知识资产问答工作台，支持：

1. 展示知识资产列表；
2. 新增知识资产；
3. 对知识资产进行检索；
4. 用户向 Agent 提问；
5. Agent 根据检索结果生成回答；
6. 展示答案和引用来源；
7. 展示简单的 Agent Trace / 检索过程；
8. 页面具备清晰、克制、有质感的 UI。

项目核心不是实现真正复杂的 AI Agent，而是完成一个最小闭环：

```txt
知识资产管理
↓
用户提问
↓
检索相关知识资产
↓
基于检索结果生成回答
↓
展示引用来源
↓
展示检索过程 / Agent Trace
```

------

## 3. 推荐技术方案

### 3.1 技术栈选择

推荐使用：

```txt
Next.js + React + TypeScript + Tailwind CSS + localStorage
```

可选增强：

```txt
shadcn/ui
Node.js API Route
JSON File
SQLite
真实 LLM API
Embedding API
```

但在 4 小时笔试场景下，不建议一开始就接入真实 LLM、真实向量数据库或复杂后端。最稳妥方案是优先完成一个高完成度 MVP。

### 3.2 推荐理由

选择 `Next.js + TypeScript + Tailwind CSS + localStorage` 的原因：

- Next.js / React 适合快速搭建 Web App；
- TypeScript 能体现工程规范；
- Tailwind CSS 方便快速实现干净 UI；
- localStorage 实现简单，可持久化；
- 不依赖后端和数据库，降低项目跑不起来的风险；
- 便于在 README 中解释技术取舍；
- 便于后续扩展到 API Route、数据库和向量检索。

------

## 4. 功能需求整理

### 4.1 知识资产列表

页面需要展示知识资产列表。每条知识资产包含：

```ts
type KnowledgeAsset = {
  id: string;
  title: string;
  content: string;
  tags: string[];
  createdAt: string;
};
```

每条资产建议以卡片形式展示：

- 标题；
- 内容摘要；
- 标签；
- 创建时间；
- 是否被检索命中。

内置至少 3 条初始数据：

```txt
1. AIOS 平台介绍
AIOS 是一个面向企业的智能体操作平台，支持知识库、工具调用、工作流编排和多智能体协作。

2. 数字资产知识库
数字资产知识库用于沉淀企业文档、业务流程、销售资料、客户案例和产品说明，并支持智能检索和问答。

3. Agent 工作流
Agent 可以通过任务拆解、工具调用、上下文记忆和结果校验完成复杂任务，但需要可观测性和权限控制来保证可靠性。
```

------

### 4.2 新增知识资产

提供一个新增知识资产表单，字段包括：

```txt
Title
Content
Tags
```

要求：

- Title 不能为空；
- Content 不能为空；
- Tags 可以用英文逗号或中文逗号分隔；
- 提交后，新资产应立即出现在知识资产列表；
- 新增的数据应写入 localStorage；
- 页面刷新后数据仍然保留；
- 表单提交后应清空输入框；
- 至少提供基本的错误提示或校验状态。

------

### 4.3 简单检索能力

实现一个基础检索逻辑，不强制接入真实向量数据库。

可采用方案：

- 关键词匹配；
- 简单分词 + 相似度评分；
- 模拟向量检索；
- 本地 embedding；
- 第三方 embedding API。

本项目建议采用：**关键词分词 + 权重评分**。

检索返回 top 3 相关知识片段，并在 UI 中展示。

返回结构：

```ts
type SearchResult = {
  assetId: string;
  title: string;
  snippet: string;
  score: number;
};
```

建议评分逻辑：

```txt
score =
标题命中次数 * 3
+ 标签命中次数 * 2
+ 正文命中次数 * 1
+ query 覆盖率 bonus
```

这样可以体现：

- title 比 content 更重要；
- tags 可以辅助主题匹配；
- content 是主要检索依据；
- score 可解释；
- top 3 结果可追踪。

------

### 4.4 Agent 问答

用户输入问题后，系统需要完成以下流程：

```txt
用户问题
↓
检索相关知识资产
↓
基于检索结果生成回答
↓
展示答案 + 引用来源
```

题目不强制接入真实 LLM，因此可以使用 mock answer，但必须体现“基于检索结果回答”的逻辑。

不建议写死固定回答，例如：

```txt
无论用户问什么，都返回同一个答案。
```

推荐做法：

```txt
1. 用户输入问题；
2. 调用 searchAssets(question, assets)；
3. 获取 top 3 检索结果；
4. 根据检索结果中的 content 生成结构化回答；
5. 展示引用来源；
6. 将 query、retrieved assets、scores、final answer 写入 trace。
```

示例：

用户问题：

```txt
AIOS 支持哪些能力？
```

检索命中：

```txt
AIOS 平台介绍
Agent 工作流
```

返回答案：

```txt
根据检索到的知识资产，AIOS 主要支持知识库、工具调用、工作流编排和多智能体协作等能力。同时，Agent 可通过任务拆解、上下文记忆和结果校验完成复杂任务，但在企业场景中需要可观测性和权限控制来保证可靠性。
```

引用来源：

```txt
引用 1：AIOS 平台介绍
引用 2：Agent 工作流
```

------

### 4.5 Agent Trace / 检索过程

页面需要展示一个简单的 Agent Trace 或检索过程区域。

包含：

```txt
Query：用户问题
Retrieved Assets：命中的知识资产
Scores：相似度分数
Final Answer：最终答案
```

Trace 的目的不是复杂，而是体现开发者理解：

```txt
AI 系统不是黑盒，检索、命中、打分和生成过程都应该可观测。
```

建议 Trace 展示格式：

```txt
Query
AIOS 支持哪些能力？

Retrieved Assets
1. AIOS 平台介绍，score: 8.5
2. Agent 工作流，score: 4.2

Final Answer
根据检索到的知识资产，AIOS 主要支持……
```

------

### 4.6 UI 要求

页面需要有基本产品完成度。

建议做到：

- 布局清晰；
- 层级分明；
- 视觉克制；
- 卡片式设计；
- 有明显的输入区、结果区、Trace 区；
- tag 使用 badge 样式；
- score 使用小数字、标签或进度条展示；
- Empty / Loading / Error 状态至少覆盖一部分；
- 不要像普通 demo 一样粗糙堆砌。

推荐页面布局：

```txt
顶部：
AI Knowledge Asset Workbench
企业知识资产检索与 Agent 问答工作台

左侧：
知识资产列表
新增知识资产表单

中间：
搜索框
检索结果 top 3
Agent 问答输入框
最终答案
引用来源

右侧：
Agent Trace
Query
Retrieved Assets
Scores
Final Answer
```

如果页面空间不够，也可以采用上下结构：

```txt
知识资产管理
↓
检索区
↓
Agent 问答区
↓
引用来源
↓
Agent Trace
```

------

## 5. 推荐项目结构

建议不要把所有代码堆在一个文件里。

推荐目录结构：

```txt
src/
  app/
    page.tsx
    globals.css

  components/
    AssetList.tsx
    AssetCard.tsx
    AssetForm.tsx
    SearchPanel.tsx
    ChatPanel.tsx
    TracePanel.tsx
    EmptyState.tsx

  lib/
    initialAssets.ts
    search.ts
    answer.ts
    storage.ts
    utils.ts

  types/
    asset.ts
```

### 5.1 类型定义

```ts
export type KnowledgeAsset = {
  id: string;
  title: string;
  content: string;
  tags: string[];
  createdAt: string;
};

export type SearchResult = {
  assetId: string;
  title: string;
  snippet: string;
  score: number;
};

export type AgentTrace = {
  query: string;
  retrievedAssets: SearchResult[];
  finalAnswer: string;
  createdAt: string;
};
```

### 5.2 核心模块职责

```txt
types/asset.ts
定义 KnowledgeAsset、SearchResult、AgentTrace 等类型。

lib/initialAssets.ts
维护初始 3 条知识资产数据。

lib/storage.ts
封装 localStorage 读写逻辑。

lib/search.ts
实现 searchAssets 方法，负责检索和打分。

lib/answer.ts
实现 generateAnswer 方法，基于检索结果生成回答。

components/AssetList.tsx
展示知识资产列表。

components/AssetForm.tsx
新增知识资产表单。

components/SearchPanel.tsx
展示搜索输入框和 top 3 检索结果。

components/ChatPanel.tsx
用户向 Agent 提问并展示回答、引用来源。

components/TracePanel.tsx
展示 Agent Trace。
```

------

## 6. 核心实现设计

### 6.1 知识资产存储设计

当前版本使用 localStorage 存储知识资产。

数据结构：

```ts
const STORAGE_KEY = "knowledge_assets";
```

初始化逻辑：

```txt
1. 页面加载时读取 localStorage；
2. 如果 localStorage 中没有数据，则使用内置初始数据；
3. 新增资产后，将完整 assets 数组写回 localStorage；
4. 页面刷新后重新读取 localStorage。
```

选择 localStorage 的原因：

```txt
1. 实现成本低，适合 4 小时笔试；
2. 不需要后端服务；
3. 可以支持页面刷新后的数据保留；
4. 便于演示新增和检索流程；
5. 后续可以平滑替换为 API + 数据库。
```

------

### 6.2 检索逻辑设计

检索函数：

```ts
searchAssets(query: string, assets: KnowledgeAsset[]): SearchResult[]
```

流程：

```txt
1. 对 query 进行标准化：
   - trim；
   - 转小写；
   - 按空格、中英文标点进行简单分词。

2. 对每个 asset 计算 score：
   - title 命中权重最高；
   - tags 命中权重中等；
   - content 命中权重基础；
   - 命中词越多，分数越高。

3. 生成 snippet：
   - 如果正文中包含 query 或关键词，则截取相关片段；
   - 如果没有明显命中，则截取 content 前一部分。

4. 按 score 降序排序。

5. 返回 top 3。
```

推荐权重：

```txt
title: 3
tags: 2
content: 1
```

这样设计的优点：

```txt
1. 简单可解释；
2. 不依赖外部服务；
3. 能体现检索排序；
4. 方便展示 score；
5. 未来可以替换为 embedding 检索。
```

------

### 6.3 Agent 回答生成设计

回答生成函数：

```ts
generateAnswer(query: string, results: SearchResult[], assets: KnowledgeAsset[]): string
```

当前版本不接真实 LLM，而是采用基于检索结果的模板化生成。

生成逻辑：

```txt
1. 如果没有检索结果：
   返回“未找到足够相关的知识资产，建议补充资料或换一个问题。”

2. 如果存在检索结果：
   - 找到对应知识资产；
   - 提取 top 1~3 的 content；
   - 拼接为结构化回答；
   - 在回答中体现“根据检索到的知识资产”；
   - 不编造检索结果之外的信息。
```

示例回答：

```txt
根据检索到的知识资产，AIOS 是一个面向企业的智能体操作平台，主要支持知识库、工具调用、工作流编排和多智能体协作等能力。同时，Agent 可以通过任务拆解、上下文记忆和结果校验完成复杂任务，但需要可观测性和权限控制来保证可靠性。
```

------

### 6.4 引用来源设计

每个 SearchResult 包含 assetId，因此可以通过 assetId 找到原始知识资产。

引用来源展示：

```txt
引用 1：AIOS 平台介绍
引用 2：Agent 工作流
```

设计重点：

```txt
1. 引用来源必须来自检索结果；
2. 不要展示不存在的来源；
3. 引用顺序与检索结果排序一致；
4. 让用户知道答案依据来自哪里。
```

------

### 6.5 Agent Trace 设计

Trace 数据结构：

```ts
type AgentTrace = {
  query: string;
  retrievedAssets: SearchResult[];
  finalAnswer: string;
  createdAt: string;
};
```

Trace 展示内容：

```txt
Query：用户问题
Retrieved Assets：命中的知识资产
Scores：每个命中资产的相似度分数
Final Answer：最终答案
```

Trace 的产品意义：

```txt
1. 降低 AI 系统黑盒感；
2. 便于用户理解答案来源；
3. 方便开发者调试检索逻辑；
4. 为未来日志审计和可观测性打基础。
```

------

## 7. 4 小时开发计划

### 第 1 小时：项目初始化与基础结构

目标：

```txt
完成项目骨架，保证页面能运行。
```

任务：

```txt
1. 初始化 Next.js 项目；
2. 配置 TypeScript 和 Tailwind CSS；
3. 定义 KnowledgeAsset、SearchResult、AgentTrace 类型；
4. 编写初始 3 条知识资产；
5. 完成基础页面布局；
6. 实现 localStorage 读取和初始化逻辑。
```

阶段成果：

```txt
页面可以打开，并展示初始知识资产列表。
```

------

### 第 2 小时：知识资产管理与检索

目标：

```txt
完成资产新增和 top 3 检索。
```

任务：

```txt
1. 实现 AssetList；
2. 实现 AssetForm；
3. 支持新增资产；
4. 新增后写入 localStorage；
5. 实现 searchAssets；
6. 展示检索结果；
7. 展示 score 和 snippet。
```

阶段成果：

```txt
用户可以新增知识资产，并通过搜索框检索 top 3 相关资产。
```

------

### 第 3 小时：Agent 问答、引用来源与 Trace

目标：

```txt
完成本题最核心的 RAG / Agent 闭环。
```

任务：

```txt
1. 实现 Agent 问答输入框；
2. 用户提问后调用检索逻辑；
3. 基于检索结果生成回答；
4. 展示最终答案；
5. 展示引用来源；
6. 生成并展示 Agent Trace。
```

阶段成果：

```txt
用户可以提问，系统可以基于检索结果回答，并展示引用和 Trace。
```

------

### 第 4 小时：UI 打磨、README 与提交

目标：

```txt
提升产品感，并完成提交材料。
```

任务：

```txt
1. 优化页面布局；
2. 优化卡片、按钮、输入框、badge；
3. 增加 Empty / Error / Loading 状态；
4. 检查代码结构和组件命名；
5. 编写 README；
6. 说明启动方式；
7. 说明技术取舍；
8. 说明未完成事项；
9. 说明继续迭代计划；
10. GitHub 提交。
```

阶段成果：

```txt
项目可运行，README 完整，功能闭环清晰。
```

------

## 8. README 编写建议

README 至少包含：

```txt
1. 项目简介；
2. 功能说明；
3. 技术栈；
4. 启动方式；
5. 项目结构；
6. 核心设计说明；
7. 技术取舍；
8. 未完成事项；
9. 后续迭代计划；
10. 题目要求的 5 个问题回答。
```

------

## 9. README 核心问题参考答案

### 9.1 如何设计知识资产的数据结构？

```md
知识资产使用 KnowledgeAsset 作为核心数据结构，包含 id、title、content、tags、createdAt 字段。

- id：唯一标识，用于检索结果和引用来源关联；
- title：资产标题，方便用户识别知识来源；
- content：知识正文，是检索和问答的主要依据；
- tags：用于补充分类、主题和关键词匹配；
- createdAt：用于排序和展示资产创建时间。

当前版本使用 localStorage 持久化，便于在无后端环境下快速验证产品流程。
```

------

### 9.2 如何实现检索？

```md
当前版本实现了一个轻量级关键词相似度检索函数 searchAssets。

检索逻辑包括：

1. 对 query、title、content、tags 做统一标准化处理；
2. 对 query 进行简单分词；
3. title 命中给予较高权重；
4. tags 命中给予中等权重；
5. content 命中给予基础权重；
6. 按 score 降序返回 top 3；
7. 为每条结果生成 snippet，帮助用户理解为什么命中。

这种方式不依赖外部服务，适合 4 小时笔试场景，同时保留了未来替换为 embedding 检索的接口边界。
```

------

### 9.3 如果要接入真实向量数据库，会怎么改？

```md
如果接入真实向量数据库，我会保留当前 KnowledgeAsset 和 SearchResult 的上层结构，但替换检索层实现。

改造方向：

1. 新增文档切分逻辑，将 content 拆分为多个 chunk；
2. 对每个 chunk 生成 embedding；
3. 将 chunk、assetId、title、tags、tenantId、createdAt 等 metadata 写入向量数据库；
4. 用户提问时，对 query 生成 embedding；
5. 在向量数据库中执行 topK 相似度检索；
6. 将检索结果转换为统一的 SearchResult；
7. 将 SearchResult 传给回答生成模块；
8. 前端仍然使用现有的检索结果和 Trace 展示结构。

可选技术包括 pgvector、Milvus、Qdrant、Pinecone、Weaviate 或 Elasticsearch 向量检索。
```

------

### 9.4 如果要支持多租户，会怎么改？

```md
如果支持多租户，需要在数据、权限和检索链路中增加 tenantId，并保证任何查询和检索都不能跨租户访问数据。

主要改造：

1. KnowledgeAsset 增加 tenantId、createdBy、visibility 字段；
2. 所有新增、查询、检索操作都必须携带 tenantId；
3. 后端接口基于登录态校验用户所属租户；
4. 数据库查询必须强制加 tenantId 条件；
5. 向量数据库中的 chunk metadata 也必须写入 tenantId；
6. 检索时必须增加 tenantId filter；
7. 增加用户、角色、部门和权限模型；
8. 管理端支持租户级配置和权限管理；
9. 日志中记录用户、租户、query、命中资产和回答结果。

多租户场景中最重要的不是 UI，而是数据隔离、权限控制和审计能力。
```

------

### 9.5 如果系统上线到真实 ToB 场景，最担心什么？

```md
如果上线到真实 ToB 场景，我最担心以下问题：

1. 数据权限隔离
   不同租户、部门、角色之间不能检索到不该看的资料。

2. 回答可靠性
   Agent 不能编造答案，需要基于检索结果回答，并展示引用来源和置信度。

3. 知识更新
   企业文档经常变化，需要处理版本更新、过期内容、删除同步和索引重建。

4. 可观测性
   需要记录 query、命中片段、score、final answer 和用户反馈，方便排查问题。

5. 安全合规
   企业知识资产可能包含合同、客户、报价、技术方案等敏感信息，需要做好访问控制、日志审计和数据脱敏。

6. 成本与性能
   embedding、LLM 调用和向量检索都可能带来成本和延迟，需要控制响应速度和调用成本。

7. 评估体系
   需要评估检索命中率、回答准确率、引用正确率和用户满意度，否则系统很难持续优化。
```

------

## 10. 未完成事项说明示例

如果时间有限，可以在 README 中如实说明：

```md
由于笔试时间限制，当前版本主要完成知识资产管理、关键词检索、基于检索结果的 mock answer、引用来源和 Agent Trace 展示。

暂未完成：

1. 未接入真实 LLM；
2. 未接入真实向量数据库；
3. 未实现登录和权限控制；
4. 未实现多租户；
5. 未实现复杂文档切分和 embedding；
6. 未实现服务端数据库持久化。

这些部分在当前版本中保留了结构边界，后续可以逐步替换和扩展。
```

------

## 11. 后续迭代计划

可以在 README 中写：

```md
如果继续迭代，我会按以下方向优化：

1. 接入真实 LLM
   使用 OpenAI、Claude、通义千问、DeepSeek 等模型，根据检索结果生成更自然的回答。

2. 接入向量数据库
   将知识资产切分为 chunk，生成 embedding，并使用 pgvector、Qdrant 或 Milvus 进行语义检索。

3. 增加权限系统
   支持用户、角色、部门、租户，保证知识资产访问权限。

4. 增加多租户能力
   在资产、chunk、检索、日志中加入 tenantId，避免跨租户数据泄露。

5. 增强 Agent Trace
   展示 query rewrite、检索结果、rerank 结果、prompt、模型输出和最终答案。

6. 增加知识管理能力
   支持编辑、删除、标签筛选、批量导入、文档上传和版本管理。

7. 增加评估与反馈
   支持用户对回答点赞、点踩、标注错误引用，用于后续优化检索和回答质量。

8. 增强安全与审计
   增加访问日志、敏感词检测、数据脱敏、权限审计和异常检索告警。
```

------

## 12. 最小可交付版本标准

本项目最低可接受完成度：

```txt
1. 可以运行；
2. 有知识资产列表；
3. 可以新增知识资产；
4. 可以检索 top 3；
5. 可以向 Agent 提问；
6. Agent 回答基于检索结果；
7. 有引用来源；
8. 有 Agent Trace；
9. UI 不粗糙；
10. README 说明完整。
```

优秀版本标准：

```txt
1. 页面采用工作台布局；
2. 组件拆分清晰；
3. TypeScript 类型完整；
4. 检索逻辑可解释；
5. 回答不是纯硬编码；
6. 引用来源和检索结果正确绑定；
7. Trace 清楚展示 query、results、scores、answer；
8. 有 Empty / Error / Loading 状态；
9. README 能体现技术判断；
10. 对向量数据库、多租户、ToB 风险有清晰说明。
```

------

## 13. 不建议优先做的内容

4 小时内不建议优先做：

```txt
1. 真实登录系统；
2. 复杂权限系统；
3. 真实向量数据库；
4. 复杂 Agent 编排；
5. 复杂后端服务；
6. 复杂数据库设计；
7. 复杂 UI 动效；
8. 过度包装的功能；
9. 大量无关页面；
10. 难以解释的 AI 生成代码。
```

原因：

```txt
题目更看重产品判断、工程结构、代码质量和交互体验，而不是功能堆砌。
```

------

## 14. 最容易扣分的问题

### 14.1 只有聊天框，没有检索链路

如果只做一个聊天框，没有知识资产列表、检索结果、引用来源和 Trace，会显得没有理解题目核心。

### 14.2 Mock answer 太假

如果所有问题都返回同一个固定答案，会被认为没有体现“基于检索结果回答”。

### 14.3 所有代码堆在一个文件

如果所有逻辑都写在 `page.tsx`，工程结构会显得不清晰。

### 14.4 UI 太粗糙

题目明确要求前端审美和产品感，因此不能只做几个输入框和按钮。

### 14.5 README 太简单

README 是证明候选人理解能力的重要材料，尤其是题目要求回答的 5 个问题必须认真写。

### 14.6 接真实 LLM 但项目跑不起来

真实 LLM 是加分项，不是必需项。稳定可运行比强行接 API 更重要。

------

## 15. AI Coding 工具使用说明

题目明确允许使用 AI Coding 工具，但最终代码需要候选人能够解释清楚。

因此可以使用：

```txt
Cursor
GitHub Copilot
Claude Code
ChatGPT
Codex
其他 AI Coding 工具
```

但需要注意：

```txt
1. 不能完全不理解生成代码；
2. README 中的设计说明要和代码一致；
3. 面试时需要能解释数据结构、检索逻辑、回答生成逻辑和 Trace 设计；
4. 不要提交大量无用代码；
5. 不要让项目看起来像一键生成的模板。
```

如需在 README 中说明，可以写：

```md
本项目开发过程中允许使用 AI Coding 工具辅助生成部分组件和样式，但核心数据结构、检索逻辑、Agent 回答流程、引用来源展示和 Trace 设计均经过人工设计与调整。所有实现细节均可解释，并在 README 中说明了当前版本的技术取舍与后续演进方案。
```

也可以不主动强调 AI 使用情况，除非对方明确要求。

------

## 16. 推荐项目名称与定位

推荐项目名称：

```txt
AI Knowledge Asset Workbench
```

或者：

```txt
AIAgent Knowledge QA Console
```

项目一句话介绍：

```txt
一个面向企业知识资产的轻量级检索增强问答工作台，支持知识资产管理、相似度检索、基于检索结果的 Agent 回答、引用来源和检索 Trace 展示。
```

------

## 17. 总体开发策略

本题最优策略不是炫技，而是做一个稳定、完整、可解释的版本。

优先级如下：

```txt
第一优先级：跑得起来
第二优先级：功能闭环完整
第三优先级：检索链路可解释
第四优先级：UI 有产品感
第五优先级：README 写清楚
第六优先级：加分功能
```

最终要让评审看到：

```txt
1. 候选人理解 RAG / Agent 的基本链路；
2. 候选人能在有限时间内做出可运行的工程；
3. 候选人知道如何设计数据结构和模块边界；
4. 候选人知道真实 ToB 系统要关注权限、安全、可靠性和可观测性；
5. 候选人不是只会堆功能，而是有产品判断和工程审美。
```

------

## 18. 提交前检查清单

提交前检查：

```txt
功能检查：
[ ] 页面可以正常启动
[ ] 展示至少 3 条初始知识资产
[ ] 可以新增知识资产
[ ] 新增后列表更新
[ ] 刷新后数据不丢失
[ ] 可以搜索知识资产
[ ] 返回 top 3 检索结果
[ ] 展示 snippet
[ ] 展示 score
[ ] 可以向 Agent 提问
[ ] 回答基于检索结果生成
[ ] 展示引用来源
[ ] 展示 Agent Trace
[ ] 空结果有提示
[ ] 表单有基本校验

工程检查：
[ ] TypeScript 类型清晰
[ ] 组件命名合理
[ ] 检索逻辑单独封装
[ ] 回答生成逻辑单独封装
[ ] localStorage 逻辑单独封装
[ ] 没有大量无用代码
[ ] 没有明显 console error
[ ] 没有提交 node_modules
[ ] README 完整

UI 检查：
[ ] 布局清晰
[ ] 卡片层级明确
[ ] 按钮、输入框样式统一
[ ] 标签展示清楚
[ ] Trace 区域容易理解
[ ] 页面不粗糙
[ ] 视觉风格干净克制

README 检查：
[ ] 有项目简介
[ ] 有启动方式
[ ] 有技术栈说明
[ ] 有技术取舍
[ ] 有未完成事项
[ ] 有后续迭代计划
[ ] 回答了题目要求的 5 个问题
```

------

## 19. 结论

这个笔试题本质上不是考复杂算法，也不是考真实大模型接入，而是考察候选人是否理解企业 AI Agent 产品的最小闭环。

推荐实现一个：

```txt
轻量、稳定、结构清晰、UI 干净、检索链路可解释的知识资产问答工作台。
```

最重要的是完成以下闭环：

```txt
知识资产
↓
检索
↓
基于检索结果回答
↓
引用来源
↓
Agent Trace
```

在 4 小时内，稳定完成这个闭环，并用 README 把技术取舍和后续演进讲清楚，比盲目接入真实 LLM 或复杂数据库更有价值。